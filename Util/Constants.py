#!/usr/bin/env python

import os

CONFIGDIR = u'.GoogleDriveSync'
CONFIGSTORE = CONFIGDIR + os.sep + u'GoogleDriveSync.cfg'
CREDENTIALS = CONFIGDIR + os.sep + u'credentials.pkl'
SYNCIGNORE = CONFIGDIR + os.sep + u'syncignore'
